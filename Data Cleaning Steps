## This file holds the commands to read in the raw data, clean the data, adjust for null values
## and creates the data subset varialbles






#Imports the data without headers - in raw form, my headers are currently long and spaced questions which will need to be
#replaced prior to puting it into the model

workdata_raw <- read.csv('returntowork5112021.csv', header = TRUE)

#Displays the first 5 lines of the data
head(workdata_raw)

